# Phase 15 — Acknowledgements (removed)

**Status:** Removed in favour of the **Training** module (`/my-training`, `training:complete` permission).

Legacy staff SOP acknowledgements (campaigns, assignments, `/my-acknowledgements`, `sop_acknowledgement_*` tables) were deleted. Use acknowledge-only training assignments instead.

See `docs/PHASE6_TRAINING.md` and migration `202606090001_drop_acknowledgements.sql`.
